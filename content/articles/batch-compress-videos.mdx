export const meta = {
  title: 'How to Batch Compress Videos',
  description:
    'Drop a folder of videos, set a size or quality once, download a ZIP. What works in a browser, what settings to use, and when HandBrake is faster.',
  lastUpdated: '2026-08-14',
  faq: [
    {
      q: 'Can I compress multiple videos at once?',
      a: 'Yes. Drop the whole set into ConvertYard’s video compressor, set the level or size target once, and it encodes them one after another. Several outputs come back as a ZIP. There is no file-count cap.',
    },
    {
      q: 'Does a batch upload the videos?',
      a: 'Not here. Each file is encoded in the tab with ffmpeg.wasm. In the Network tab you should not see a POST the size of your videos. The no-upload guide has the exact check.',
    },
    {
      q: 'How many videos can I do in one go?',
      a: 'Whatever the tab can hold and you can leave open. A handful of short clips is easy. A folder of 4K files belongs on a desktop, and you may want to split it. On a phone, do one large file at a time.',
    },
    {
      q: 'Is batch quality worse than doing files one by one?',
      a: 'No. The same CRF, codec, resolution, and audio settings apply to every file. A clip in a batch of 20 matches that clip compressed alone.',
    },
    {
      q: 'Why is a batch so slow?',
      a: 'Video encode is per-frame. A 3-minute 1080p30 clip is about 5,400 frames. Files run one after another so two uncompressed videos do not sit in memory at once. Keep the tab in the foreground.',
    },
    {
      q: 'Can every file in the batch share a 25 MB limit?',
      a: 'Yes. Target size 25 MB (or the 25 MB page), then drop the set. Each file is measured on its own. Files already under the cap are remuxed, not re-encoded.',
    },
  ],
  relatedTools: ['compress-video', 'extract-audio', 'video-trimmer', 'mp4-to-mp3'],
}

Compressing one video? Select the file, choose settings, then upload. Twenty videos? Select files, wait 10 to 20 minutes for compression, then download the ZIP file.

Batch compression: drop the folder, set the knobs once, download a ZIP.

**[Open the video compressor →](/compress-video/)**

## When this is the right job

- A week of lectures has to go out as email attachments.
- Can't send fifteen screen recordings on Slack.
- Every file should be below a specific size limit: 25 MB on Gmail, 10 MB on Messenger.
- A batch of iPhone HEVC clips needs to be converted to H.264 MP4s for people using older PCs.

An 8-minute-long video? Skip batch framing and [compress that file](/compress-video/).

## Three ways to compress a batch of videos

**HandBrake (Open-Source Video Transcoder).** Local, hardware-encoded with a real queue. It's a good option if you're already working in these apps. But it's very heavy for one-time folder compression.

**Desktop-Based FFmpeg.** Fastest option for a large directory. Nothing to upload. No limit on the number of videos to process. However, you need FFmpeg installed, and you need to know what to do when a file doesn't have a video track.

**Compress in the Browser, locally.** I've compiled FFmpeg to run as a WebAssembly instance within your browser tab. I process one file at a time to avoid loading the tab with two uncompressed videos and two outputs. No installation is required. Whilst ConvertYard is accessible from a phone's browser, a phone is the wrong machine for processing a folder of 200 MB clips. A desktop browser is more suitable.

## Steps

1. Open [convertyard.com/compress-video](/compress-video/). Choose the correct settings. For example, if you need to send videos on Gmail, choose the 'to 25 MB' target file size.
2. Select videos you want to compress. Or drop them into the dropzone. I've tested this extensively (up to 30 files under 500 MB), and it finished compressing in under 30 minutes. Please note that the output will be in MP4 only, as it's the most compatible across platforms (Windows, Mac, iOS, and Android).
3. Choose the appropriate settings for your needs — these will apply to every file.
4. Don't close the tab (especially iPhone users), and wait for the compression to finish.
5. Download the ZIP or individual file. File names are automatically generated.

## One policy per drop

If half the files need High + 720p and the other half need Medium at original size, do two drops.

| Destination | Start here |
|---|---|
| Gmail / generic email | Target 25 MB. 720p if the source is 1080p or 4K. |
| Strict form (10 MB) | Target 10 MB. 720p or 480p. H.265 only if playback is on phones. |
| Internal Slack / Drive, quality first | Medium, original resolution, H.264. |
| Rough proxies / archive | High or Maximum, 720p. |
| Mixed old hardware | H.264, Medium, 720p. |
| Only recent iPhones / Androids | H.265 on, Medium. |

Sport and games shrink less than slides and talking heads. If the folder is half screen recordings and half a soccer match, split it. See [H.264 vs H.265](/blog/h264-vs-h265-video-compression/) if the codec is the open question.

## What will actually stop a batch

There is no product file-count limit. The machine is the limit.

- **Memory.** A 4K file in a WASM heap can kill the tab. Split huge files onto a desktop session.
- **Time.** Ten 3-minute clips is roughly ten times one clip. Start it when you can leave the machine alone.
- **Phones.** Fine for one or two small files. Not a field batch station.
- **Sleep / closed tab.** In-progress work is gone. Files you already downloaded are safe. This is not a cloud job you can resume.
- **A bad file.** No video track → that row fails, the rest continue.

A hundred long files overnight: HandBrake’s queue or desktop ffmpeg. The browser tool is for the folder on the computer in front of you, today, without installing anything.

How the file stays local is covered in [Compress video without uploading](/blog/compress-video-without-uploading/).

## Also useful

- [Compress video without uploading](/blog/compress-video-without-uploading/)
- [H.264 vs H.265: which to pick](/blog/h264-vs-h265-video-compression/)
- [What a browser can (and cannot) do with video](/blog/browser-video-editing-2026/)
